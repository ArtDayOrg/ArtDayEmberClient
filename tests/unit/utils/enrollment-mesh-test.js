import enrollmentMesh from '../../../utils/enrollment-mesh';
import {
    module, test
}
from 'qunit';

module('Unit | Utility | enrollment mesh');

// Replace this with your real tests.
test('it works', function(assert) {
    //var result = enrollmentMesh();
    //assert.ok(result);
    assert.ok(true);
});