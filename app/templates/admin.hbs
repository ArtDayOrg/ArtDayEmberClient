{{#if isAdmin}}
	<div class="admin-toolbar">
    <div class="admin-functions">
		  {{#link-to 'admin.students'}}Students{{/link-to}} |  
		  {{#link-to 'admin.sessions'}}Sessions{{/link-to}} | 
      {{#link-to 'admin.overview'}}Overview{{/link-to}} 
    </div>
      {{fb-widget adminName=adminName userImageUrl=userImageUrl logout='logout'}}
  </div>
{{else}}
  {{fb-login login="login"}}
{{/if}}
<div>
  {{outlet}}
</div>